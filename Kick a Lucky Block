local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")
local VirtualInputManager = game:GetService("VirtualInputManager")
local RS = game:GetService("ReplicatedStorage")

local KickRE = RS.Shared.Packages.Network.rev_KickEvent

local player = Players.LocalPlayer
local playerGui = player.PlayerGui

--// LOAD UI
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

--// WINDOW
local Window = Fluent:CreateWindow({
    Title = "Kick A Lucky Block",
    SubTitle = "Majek Hub",
    TabWidth = 160,
    Size = UDim2.fromOffset(520, 400),
    Acrylic = true,
    Theme = "Darker",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "loader" }),
	Teleport = Window:AddTab({ Title = "Teleport", Icon = "ghost" }),
}

--// STATE
local autoClickBonus = false
local bonusConnections = {}
local autoKick = false
local kickConnection = nil
local autoFarmBlock = false
local farmConnection = nil

local kickCF = CFrame.new(
    695.58844, 3.00000691, 231.934326,
    0.0143117635, 7.11090031e-09, 0.999897599,
    7.17339077e-09, 1, -7.21430293e-09,
    -0.999897599, 7.27590521e-09, 0.0143117635
)

local safeCF = CFrame.new(
    696.822266, 3.00000715, 321.051483,
	-0.0317601524, -2.77150192e-09, 0.999495506,
	4.0495105e-09, 1, 2.90157876e-09,
	-0.999495506, 4.13962198e-09, -0.0317601524
)

local WeightShop = CFrame.new(
	730.311829, 3.10000825, 193.675735,
	0.222229362, -9.29465216e-08, 0.974994421,
	9.4676281e-08, 1, 7.37508543e-08,
	-0.974994421, 7.59192389e-08, 0.222229362
)

local SpeedUpgrade = CFrame.new(
	730.760864, 3.00000691, 268.084991,
	-0.20107615, -4.26971773e-08, 0.979575634,
	-1.04490127e-07, 1, 2.21388774e-08,
	-0.979575634, -9.79043762e-08, -0.20107615
)

local SellStand = CFrame.new(
	731.449951, 3.1000061, 328.038605,
	-0.737040341, 2.73796763e-09, 0.675848722,
	1.44397394e-09, 1, -2.47644261e-09,
	-0.675848722, -8.49330106e-10, -0.737040341
)

--// ================= UI =================
local AutoClick = Tabs.Main:AddToggle("Auto", {
    Title = "Auto Click",
	Description = "Auto 2x(Bonus) while training",
    Default = false
})

local AutoKick = Tabs.Main:AddToggle("AutoKick", {
    Title = "Auto Kick",
    Description = "Auto kick when bar is full",
    Default = false
})

local AutoFarm = Tabs.Main:AddToggle("AutoFarm", {
    Title = "Auto Farm",
    Description = "Auto farm block(Auto Kick ON)",
    Default = false
})

--// FUNCTION AUTO CLICK BONUS
local function clearConnections()
    for _, conn in ipairs(bonusConnections) do
        if conn and conn.Disconnect then
            conn:Disconnect()
        end
    end
    table.clear(bonusConnections)
end

local function clickCenterScreen()
    local camera = workspace.CurrentCamera
    local viewport = camera.ViewportSize

    local x = viewport.X / 2
    local y = viewport.Y / 2

    -- klik tengah layar
    VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 0)
    task.wait(0.05)
    VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 0)
end

local function teleport(cf)
    local char = player.Character or player.CharacterAdded:Wait()
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then
        hrp.CFrame = cf
    end
end

local function clickButton(button)
    if not button or not button.Visible then return end

    VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.ButtonB, false, game)
    task.wait(0.1)
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.ButtonB, false, game)
end

local function setupBonusAutoClick()
    clearConnections()

    local kickGui = playerGui:WaitForChild("KickUpgrades")

    local function hookBonus(button)
        if not button:IsA("GuiButton") then return end

        local conn = button:GetPropertyChangedSignal("Visible"):Connect(function()
            if autoClickBonus and button.Visible then
                task.wait(0.1)
                pcall(function()
                    clickButton(button)
                end)
            end
        end)

        table.insert(bonusConnections, conn)

        -- kalau sudah visible
        if autoClickBonus and button.Visible then
            task.wait(0.1)
            pcall(function()
                clickButton(button)
            end)
        end
    end

    for _, v in ipairs(kickGui:GetDescendants()) do
        if v.Name == "Bonus" then
            hookBonus(v)
        end
    end

    local addedConn = kickGui.DescendantAdded:Connect(function(v)
        if v.Name == "Bonus" then
            task.wait()
            hookBonus(v)
        end
    end)

    table.insert(bonusConnections, addedConn)
end

local function startAutoKick()
    if kickConnection then return end

    local KickArea = workspace:WaitForChild("Areas"):WaitForChild("KickReady")

    kickConnection = game:GetService("RunService").Heartbeat:Connect(function()
        if not autoKick then return end

        local char = player.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        local distance = (hrp.Position - KickArea.Position).Magnitude

        if distance <= 20 then
            if not _G.__kickDebounce then
                _G.__kickDebounce = true

                KickRE:FireServer()

                task.delay(0.3, function()
                    _G.__kickDebounce = false
                end)
            end
        end
    end)
end

local function stopAutoKick()
    if kickConnection then
        kickConnection:Disconnect()
        kickConnection = nil
    end
end

local function startAutoFarm()
    if farmConnection then return end

    farmConnection = task.spawn(function()
        local waves = workspace:WaitForChild("Waves")

        while autoFarmBlock do
            teleport(kickCF)
            task.wait(0.2)
            KickRE:FireServer()

            local newWave
            local conn
            conn = waves.ChildAdded:Connect(function(child)
                newWave = child
				print(newWave.Name)
            end)

            repeat task.wait() until newWave or not autoFarmBlock
            if conn then conn:Disconnect() end
            if not autoFarmBlock then break end
            task.wait(0.05)

            local root
            if newWave:IsA("Model") then
                root = newWave:FindFirstChild("Root") or newWave.PrimaryPart
				teleport(safeCF)
            end

            if root then
                local pos = root.Position
                if pos.X >= 800 then
                    teleport(kickCF)
                end
            end

            repeat task.wait() until #waves:GetChildren() == 0 or not autoFarmBlock
            if not autoFarmBlock then break end
        end
    end)
end

local function stopAutoFarm()
    autoFarmBlock = false
    farmConnection = nil
end

AutoClick:OnChanged(function(v)
    autoClickBonus = v
    if v then
        setupBonusAutoClick()
    else
        clearConnections()
    end
end)

AutoKick:OnChanged(function(v)
    autoKick = v

    if v then
        startAutoKick()
    else
        stopAutoKick()
    end
end)

AutoFarm:OnChanged(function(v)
    autoFarmBlock = v

    if v then
        startAutoFarm()
    else
        stopAutoFarm()
    end
end)

Tabs.Teleport:AddButton({
    Title = "Teleport - Weight Shop",
    Description = "Move you to weight stand",
    Callback = function()
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")

        hrp.CFrame = WeightShop
    end
})

Tabs.Teleport:AddButton({
    Title = "Teleport - Run Upgrades",
    Description = "Move you to speed stand",
    Callback = function()
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")

        hrp.CFrame = SpeedUpgrade
    end
})

Tabs.Teleport:AddButton({
    Title = "Teleport - Sell's",
    Description = "Move you to sell stand",
    Callback = function()
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")

        hrp.CFrame = SellStand
    end
})


Tabs.Main:AddButton({
    Title = "Teleport",
    Description = "Move you to kick/safe zone",
    Callback = function()
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")

        hrp.CFrame = CFrame.new(696.822266, 3.00000715, 321.051483, -0.0317601524, -2.77150192e-09, 0.999495506, 4.0495105e-09, 1, 2.90157876e-09, -0.999495506, 4.13962198e-09, -0.0317601524)
    end
})
